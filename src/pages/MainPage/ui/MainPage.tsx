import { Counter } from 'entities/Counter';
import React from 'react';

const MainPage = () => {
    return (
        <div>
            MainPage
            <Counter/>
        </div>
    );
};

export default MainPage;
